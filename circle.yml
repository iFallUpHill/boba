deployment:  
  production:
    branch: "master" 
    commands:
      - "git fetch --unshallow"
      - sh ./deploy.sh
dependencies:
    override:
        - echo "dependencies"    
test:
    override:
        - echo "test"  