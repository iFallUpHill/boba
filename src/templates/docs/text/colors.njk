<div class="row">
    <div class="col xs-12">
        <h1 class="header">Colors</h1>
        <h5 class="subheader thin no-margin">A splash of color to make your text stand out using boba's fresh colors.</h5>
    </div>
    <div class="col xs-12">
        <table class="bordered">
            <tbody>
                <tr>
                    <td rowspan="{{ (global_color_list_core | length) + (global_color_list_base | length) + 2 }}"><span class="bold">Colors</span></td>
                </tr>
                <tr>
                    <td colspan="3">To change the color of your text, add a <code class="inline">.text-{COLOR}</code> class.</td>
                </tr>
                {% for color in global_color_list_core %}
                <tr>
                    <td><code class="inline">.text-{{ color }}</code></td>
                    <td>Add this class to make your text {{ color }}.</td>
                    <td><span class="text-{{ color }}">I'm {{ color }} text!</span></td>
                </tr>
                {% endfor %}
                {% for color in global_color_list_base %}
                <tr>
                    <td><code class="inline">.text-{{ color }}</code></td>
                    <td>Add this class to make your text {{ color }}.</td>
                    <td class="{{ "bg-dark lighten-bg-2" if (color == "light") or (color == "white") }}"><span class="text-{{ color }}">I'm {{ color }} text!</span></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
