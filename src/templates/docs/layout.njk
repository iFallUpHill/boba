<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" name="viewport" content="width=device-width, initial-scale=1">
        <title>{{ page_title }} | Build with Boba</title>
        <link rel="stylesheet" href="/boba.min.css">
        <link rel="stylesheet" href="/documentation.css">
    </head>
    <body>
        <div class="sidebar-layout">

            {% import "templates/docs/core/_topsidenav.njk" as sidebar %}
            {{ sidebar.generate(active_accordion, active_page) }}
            
            <a class="sb-main-overlay" href="#close"></a>

            <div class="sb-main-content">
                {% import "templates/docs/core/_header.njk" as header %}
                {{ header.generate(active_accordion) }}

                <div class="section">
                    <div class="container">
                        {% set global_color_list_core = ['red' ,'orange', 'yellow', 'green', 'teal', 'blue', 'indigo', 'purple', 'pink'] %}
                        {% set global_color_list_base = ['dark' ,'light', 'black', 'white'] %}
                        {% block main %}
                        {% endblock %}
                    </div>
                </div>
            </div>
        </div>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/9.12.0/highlight.min.js"></script>
        <script>hljs.initHighlightingOnLoad();</script>
    </body>
</html>
