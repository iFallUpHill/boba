<div class="row">
    <div class="col xs-12">
        <h4 class="header">Inverted</h4>
        <h6 class="subheader thin no-margin">Use the <code class="inline">.inverted</code> class to have the color of your button appear on hover and click.</h6>
    </div>
    <div class="col xs-12 md-6 col-middle">
        <div>
            <button class="inverted mv-1">Default</button>
            <br />
            {% for color in global_color_list_core %}
            <button class="{{ color }} inverted mv-1">{{ color | capitalize }}</button>
            {% endfor %}
        </div>
    </div>
    <div class="col xs-12 md-6 col-middle">
        <div class="code-block">
            <pre class="code"><code class="language-html">&lt;button class="green inverted"&gt; Green &lt;/button&gt;</code></pre>
        </div>
    </div>
</div>